// queryErrorHandler function to use in the future
// import { createStandaloneToast } from '@chakra-ui/react';

// const toast = createStandaloneToast();

// export function queryErrorHandler(error: unknown): void {
//   const title =
//     typeof error === 'string' ? error : 'error connecting to server';
//   toast({ title, status: 'error', variant: 'subtle', isClosable: true });
// }

// to satisfy typescript until this file has contents
export {};
